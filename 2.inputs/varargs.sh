#!/usr/bin/bash

# shell inputs get the strings after the file name in run time,
# and puts in in variables from 1 to n. We can access them by $n.

echo "Total input: $#"
echo "First input: $1"
echo "All inputs: $*"
